[core]
	editor = vim
	pager = bat
	excludesfile = ~/.gitignore_global
[user]
	name = dino
	email = sho.edu.human@gmail.com
[color]
    ui = auto
[credential]
	helper = osxkeychain
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
